# 2026.199.1330 — agent control-plane packaging boundary

- Document that typed tmux terminal and agent-runtime providers compile into
  Charly and do not ship as a separate baked command plugin.
- Remove the stale `plugin-tmux` entry from the baked-plugin inventory. The
  existing dependency list already supplies the required runtime tools, so
  this cutover adds no package dependency.
- Honor the local-package builder's explicit source-root input while retaining
  the standalone fallback. Arch `makepkg` now works from the SDK's isolated
  package-definition copy without losing the committed superproject, SDK,
  binary, or plugin inputs it packages.
