# gitignore the opencharly-sdk makepkg second-source clone

The PKGBUILD's second git+file:// source (opencharly-sdk, the sdk submodule
wiring) leaves an opencharly-sdk/ clone in the build dir, exactly like the
existing opencharly-git/ main-source clone. Add it to .gitignore so it never
shows as untracked.
