# 2026.183.1908

## 2026-07-02 — Refresh `.SRCINFO`: drop stale pre-rebrand source path

`makepkg --printsrcinfo` regenerated `pkg/arch/.SRCINFO`, dropping the stale
pre-rebrand local `source =` path (`…/Atrapub/overthink`) left by the genesis
rename. The committed snapshot now matches the PKGBUILD's
`git+file://$(realpath …)` source for the current checkout. Build/packaging
behavior is unchanged (the local `-git` build reads PKGBUILD, not `.SRCINFO`).
