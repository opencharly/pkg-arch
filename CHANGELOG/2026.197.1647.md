# 2026.197.1647 — sweep stale `task build:charly`/`task install`/`install-arch` refs

The superproject's binary-policy cutover (#131) deleted the `task build:charly` /
`task install` / `install-arch` Taskfile targets — `task build:binary` (per-worktree
dev build, never touches the host) and `task build:install-portable` are the only
surviving dev-workflow tasks. This repo's own docs/comments still named the
deleted tasks, a cross-repo R5 remnant the superproject's own grep self-test
cannot reach (this is a separate gitlink). Full sweep (`grep -rn
'build:charly\|task install\b|install-arch' pkg/arch/`), each replacement verified
against the CURRENT `taskfiles/Build.yml` before writing it:

- `PKGBUILD` (3 lines: the `go-task` optdepends description, the `curl`
  makedepends comment, and the `pkgver()` doc comment) → `task build:binary` /
  `task build:install-portable`.
- `.SRCINFO` — the ONE line that mirrors PKGBUILD's `go-task` optdepends text,
  hand-patched to match (a full `makepkg --printsrcinfo` regeneration was tried
  first and reverted — it pulled in build-machine-local noise unrelated to this
  fix: the ephemeral worktree's absolute path in `source=` and an unrelated
  pre-existing `pkgver` drift, neither of which this cutover's claim depends on).
- `opencharly-git.install` — the post_install symlink-setup comment.
- `calver.sh` — two comments citing the ldflags-injection task by name.
- `README.md` — the "Install" section's alternate one-liner claimed a repo-root
  task that ran `makepkg -sefi`; that task doesn't exist in any form any more
  (`task build:binary` never installs a pacman package; `task build:pkg:arch`
  only builds the `.pkg.tar.zst` artifact into `dist/`), so a mechanical rename
  would have created a NEW false claim — rewrote the paragraph to state that
  directly instead.

All five are comment/doc-only edits — no `depends=`/`makedepends=`/`build()`/
`package()` line's VALUE changed, only descriptive text. Verified: `bash -n` on
`PKGBUILD`/`opencharly-git.install`/`calver.sh` (syntax intact); `git grep
'build:charly\|task install\b|install-arch'` in this repo now returns only this
repo's own `CHANGELOG/2026.189.0135.md` historical entry.

*Assisted-by: Claude (documentation reviewed)*
