# 2026.215.1924 — fix(pkg): PKGBUILD go build passes -buildvcs=false (charly#178 fourth surface)

The localpkg guest install (the check-local-vm R10 bed) failed in `build()`: makepkg's srcdir
checkout hits Go's deterministic VCS-status-walk failure ("error obtaining VCS status: exit
status 128") — the same charly#178 root cause previously fixed for production plugin builds
(`pluginBuildVCSFlagForContext`), the Taskfile binary build, and the charly test helpers. Both
`go build` invocations (the charly binary and the per-plugin builds) now pass
`-buildvcs=false` with the standing-rule comment: every charly binary build — production,
test, Taskfile, and packaging — carries the flag; the VCS stamp has zero consumers.

R10: proven end-to-end by check-local-vm PASS 8/8 on the #55 terminus roster (the guest's
opencharly-git makepkg build succeeds against exactly this PKGBUILD; evidence in
opencharly/charly#204).
